version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./src:/app/src
      - ./summit_housing.db:/app/summit_housing.db
    environment:
      - PYTHONUNBUFFERED=1
